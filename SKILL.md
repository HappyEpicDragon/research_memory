---
name: research-memory
description: 科研项目的记忆恢复 + 实验执行约定——维护 .research/state.md 作为唯一的"当前状态"文件（人话写成、有硬性长度上限、强制整篇重写而非追加），决策关卡调用更强的判断模型（advisor / Codex）而执行留在主线 agent，新实验按 Hydra 骨架落盘，远程/本地算力从 .research/compute_nodes.md 读取。Use when the user asks to 恢复上下文 / resume a research project / 继续之前的实验 / 开始新实验 / 记录进展 / 这个项目现在到哪了.
---

# research-memory

科研项目的轻量记忆与实验执行约定。只做一件事：让长会话之间的上下文恢复**又小又能看懂**，让实验**有地方跑、有地方存**。不做文献调研、不做论文写作、不做 review——那些不在这个 skill 的范围内。

## 目录约定

```
.research/
  state.md              # 唯一的"当前状态"文件，见下方核心规则
  decisions.log.md       # 追加型日志：被否决/确认的决策，一行一条
  experiments.log.md     # 追加型日志：每次实验跑的一行索引，指向 outputs/，不复制内容
  compute_nodes.md       # 手动维护的算力登记表，见"算力"一节
  archive/                # 压缩 state.md 时被替换掉的旧快照，默认不加载

conf/                    # Hydra 配置组（dataset / model / train / eval ...）
outputs/                 # Hydra 自动管理的运行产物，按 <date>/<time> 自动分目录
```

首次在一个项目里使用本 skill、且 `.research/` 不存在时，**不要**直接用空模板起步——先跑 `/research-init`：全量扫描仓库（git log、README、目录结构、已有实验代码/结果）+ 问用户几个代码看不出来的问题（项目在研究什么、现在到哪一步、有没有已知的坑），再落盘。空模板只在用户明确说"不用扫描，直接给我空的"时才用。

## 核心规则一：state.md 只能整篇重写，不能 Edit 追加

`state.md` 是唯一承担"恢复上下文"职责的文件。每次要更新它，遵守：

1. **用 Write 整篇重写，不用 Edit 插入/追加。** 把这次更新当成"重新写一遍现在的状态"，不是"在旧文档上打补丁"。
2. **旧的、已经被取代的结论直接删除**，不要保留、也不要用"本节优先于以下所有内容"这类堆叠式标注。如果某段历史还有查证价值，放进 `decisions.log.md`（可查证的一行）或 `archive/<date>.md`（完整原文），`state.md` 本身永远只反映当前情况。
3. **硬性长度上限：200 行。没有任何脚本会替你检查这一条**——每次写完 `state.md`，必须自己数一下行数（比如 `wc -l .research/state.md`）；超了当场删/压缩再落盘，不要写完就当完事了。把被替换下来的完整版本存一份到 `archive/<今天日期>.md`。
4. **面向人类冷启动阅读，不是面向模型自己检索。** 假设读者是刚接手项目、什么都不知道的人：
   - 文件顶部固定一个 `## 术语表` 区块。任何编号/代号（实验 ID、决策 ID、任务 ID……）第一次在正文出现，必须先在术语表里给出全称 + 一句话说明。
   - 正文用完整句子说清楚"现在是什么状态、为什么、接下来打算干什么"，不要只贴公式/符号堆叠。允许保留必要的技术细节和数学表达，但每一处都要有对应的人话解释，不能让读者靠猜。
   - 检验标准：一个完全不知道项目背景的人，只读这一个文件，应该能看懂项目现在在哪一步、上一步做了什么、下一步打算做什么。

`decisions.log.md` 和 `experiments.log.md` 是追加型日志，允许一直增长——这是它们该有的样子。但**读取时默认只取最后 20 条**，不要为了"恢复上下文"把整个日志文件全部塞进对话。需要更早的记录时再针对性去查，不要默认全量加载。

## 核心规则二：执行留在主线，决策才升级

写代码、跑实验、调试报错——这些都由当前主线 agent 直接做，不要因为"这是个技术任务"就转手给 Codex 或建议用户用别的工具去做。

只有在以下几个决策关卡，才主动调用 `advisor()`（更强的判断模型）或提示用户想不想用 Codex 求第二意见：
- 实验方案本身是否合理（在开始实现之前）
- 一轮结果是否达标 / 支持了想要的结论
- 是否应该改变研究方向或放弃当前路线
- 一个阶段性结论是否经得住推敲（比如要写进论文之前）

这几个点之外，不要主动提出"要不要转给 Codex"——用户明确希望执行留在这里完成。

## 核心规则三：新实验用 Hydra 骨架

写新实验代码时（不回填改造已有的旧实验代码）：

- 超参数放进 `conf/` 下的 Hydra config group（例如 `conf/dataset/`、`conf/model/`、`conf/train/`），**不要把超参编码进文件名或变量名**（不要出现类似 `run_lr0.001_bs32.py` 这种命名）。
- 训练/实验入口脚本用 `@hydra.main` 接管，让每次运行自动落到 `outputs/<date>/<time>/`，配置快照、日志、checkpoint 都在这个目录下，不用人工去对应"哪个文件对应哪组参数"。
- 每次运行结束后，在 `experiments.log.md` 追加**一行**：run 的 `outputs/...` 路径 + 一句话结论。不要把超参、完整日志复制进 ledger——真相来源永远是 `outputs/` 目录本身，ledger 只是一个可以快速扫一眼的索引。

## 核心规则四：开始实验前先看算力登记表

`compute_nodes.md` 是手动维护的远程服务器登记表，每次都完整加载（它不参与 `state.md` 的压缩/归档周期）。

开始任何会实际运行代码的实验任务之前：
1. 先读 `.research/compute_nodes.md`。
2. 用 `AskUserQuestion` 明确问一次"本地跑还是用登记表里的哪台远程"——**不要不问就默认本地执行**，也不要长会话跑到中途突然"忘了"服务器怎么连，登记表里已经写明的连接方式要一直沿用，不要要求用户重复告知。
3. 远程执行直接用 Bash 工具跑 `ssh`/`scp`，不依赖任何额外 CLI 工具或桌面应用配置。改完代码要立刻同步远程验证，不要改完就停下来等用户确认；报错先自己分析、修一轮再跑，跑不通两三轮还是不行才向用户求助。

`compute_nodes.md` 里的连接信息（账号、可能包含的密码等）由用户手动维护，明文保存，这个 skill 不做加密或脱敏处理。

## 什么时候读什么

- **项目第一次用这个 skill、`.research/` 还不存在**（也对应 `/research-init` 命令）：全量扫描仓库 + 问用户几个问题，建立初始记忆，见上一节。
- **恢复上下文 / "这个项目到哪了"**（也对应 `/research-resume` 命令）：读 `state.md`（全文，因为它本来就短）+ `compute_nodes.md`（全文）+ `decisions.log.md` 和 `experiments.log.md` 的最后 20 条。用人话跟用户复述一遍现状，不要照搬术语表之外的编号。
- **记录一次进展 / 结束一轮工作**（也对应 `/research-checkpoint` 命令）：重写 `state.md`（遵守核心规则一），必要时往 `decisions.log.md` / `experiments.log.md` 追加一行。
- **开始新实验**：先过核心规则四的算力确认，再按核心规则三的 Hydra 骨架写代码，跑完按核心规则三记 ledger。

`/research-init`、`/research-resume`、`/research-checkpoint` 是三个显式的斜杠命令（`commands/` 下同名文件），分别对应上面前三条。不用命令、靠自然语言触发本 skill 也可以，但命令更可靠，想明确触发时优先用命令。
