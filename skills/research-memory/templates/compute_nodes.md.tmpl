# 算力登记表

> 手动维护，明文保存，不做加密/脱敏。每次恢复上下文或开始实验前会被完整读取，见 `research-memory/SKILL.md` 的"核心规则四"。

| 名称 | 连接方式 | 工作目录 | 备注 |
|---|---|---|---|
| local | - | `.` | 本地执行，仅小规模 debug（示例，按需修改） |

<!--
远程服务器示例：
| lab-a100 | `ssh user@1.2.3.4 -p 22`，密码/密钥见下 | `/home/user/proj` | 8x A100，conda env `xxx` |

连接密钥/密码可以直接写在这里，或者写"密钥见 ~/.ssh/xxx，密码见 XXX"。
-->
